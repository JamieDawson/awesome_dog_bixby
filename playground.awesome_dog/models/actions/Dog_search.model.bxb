action (Dog_search) {
  type(Search)
  description (A search for dogs)
  
  collect {
    input (breed) {
      type (Dog_text_one)
      min (Optional) 
    } 
    input (subbreed) {
      type (Dog_text_two)
      min (Optional) 
    }
  }
  output (Dog_struct)
}
