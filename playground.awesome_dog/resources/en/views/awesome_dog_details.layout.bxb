layout {
  match: Dog_struct (this)
  mode (Details)

  content {
    section {
      content {
        paragraph {
          value ("testttttt")
          style (Title_L)
        }

        image {
          object-fit (Cover)
          lightbox-enabled (false)
          url("#{value(this.dog_image.url)}")
        }

      }
    }
  }
}
