layout {
  match: Dog_struct (this)
  mode (Details)

  content {
    section {
      content {

       // if (exists(this.dog_image.url)) {
          image {
            object-fit (Contain)
            lightbox-enabled (false)
            aspect-ratio(1:1)
            url("#{value(this.dog_image.url)}")
          }
      // }
      }
    }
  }
}
