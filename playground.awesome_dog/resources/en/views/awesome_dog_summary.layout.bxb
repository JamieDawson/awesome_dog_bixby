layout {
  match: Dog_struct (this)
  mode (Details)

    content {
      section {
        content {       
        image {
          object-fit (Contain)
          aspect-ratio(1:1)
          lightbox-enabled (false)
          url("#{value(this.dog_image.url)}")
        }
      }
    }
  }
}